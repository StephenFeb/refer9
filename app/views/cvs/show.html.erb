<table class="table table-bordered table-striped">
  <thead>
    <tr>
      <th>Ttile</th>
      <th>Download Here</th>
      <th>Action</th>
    </tr>
  </thead>
  <tbody>
    <% @cvs.each do |cv| %>
      <tr>
        <td><%= cv.title %></td>
        <td><%= link_to "Download Resume", cv.attachment_url %></td>
        <td><%= button_to "Delete",  cv, method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure that you wish to delete #{doc.title}?" } %></td>
      </tr>
    <% end %>
  </tbody>
</table>